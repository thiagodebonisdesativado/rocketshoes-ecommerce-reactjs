import styled from 'styled-components';
import Colors from 'styles/Colors';

const Input = styled.input`
  padding: 0.4rem;
  font-weight: bolder;
  background-color: ${Colors.greys.light1};
  border: 1px ${Colors.greys.light1} solid;
  border-radius: 4px;
  text-align: center;
`;

export default Input;
